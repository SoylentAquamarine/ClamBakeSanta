# ClamBakeSanta dependencies
# Kept intentionally minimal for fast GitHub Actions installs.

# OpenAI-compatible SDK (used for GitHub Models — free with GITHUB_TOKEN)
openai>=1.0.0

# HTTP client (Mastodon + Bluesky + Tumblr + WordPress API posts)
requests>=2.31.0

# OAuth1 signing (Tumblr API)
requests-oauthlib>=1.3.0

# Config file parsing
pyyaml>=6.0

# CMU Pronouncing Dictionary — syllable counting for haiku 5-7-5 validation
pronouncing>=0.2.0

# Astronomy calculations — moon phases, zodiac ingress, equinoxes, solstices
ephem>=4.1.0
